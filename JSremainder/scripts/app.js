import * as util from "./util";


console.log(util.apyKey);

function ceva(t)  {
    let x = t.map((item) => ({val:item}))
    return x;
}

function transformToObjects(numberArray) {
    let x = numberArray.map((item) => ({val:item}))
    return x;
}


util.numberString(util.apyKey);

